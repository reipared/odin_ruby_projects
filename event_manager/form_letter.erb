<html>
	<head>
		<title>Thank you!</title>
	</head>
	<body>
		<h1>Thanks <%= name %></h1>
    <h2>Zipcode is <%= zipcode %></h2>
		<p>
			Thanks for coming to our conference. We couldn't have done it without you!
		</p>

		<p>
			Political activism is at the hear of any democracy and your voice needs to
			be heard. Please consider reaching out to your following representatives:
		</p>

		<table>
      <%= if legislators.kind_of?(Array)%>
      <th>Name</th>
      <th>Website</th>
      <%= legislators.each do |legislators| %>
        <tr>
          <td><%="#{legislator.name}" %></td>
          <td><%="#{legislator.urls.join}" unless legislator.urls.nil? %></td>
        </tr>
      <% end %>
      <% else %>
        <th></th>
        <td><%= "#{legislators}" %></td>
      <% end %>
		</table>
	</body>
</html>
